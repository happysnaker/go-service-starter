# Service identity
SERVICE_NAME=go-service-starter

# Listen address
HTTP_ADDR=:8080

# debug | info | warn | error, or a numeric slog level
LOG_LEVEL=info

# Server timeouts
HTTP_READ_TIMEOUT=5s
HTTP_WRITE_TIMEOUT=10s

# Grace period for SIGINT / SIGTERM shutdown
SHUTDOWN_TIMEOUT=10s
